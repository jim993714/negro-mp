# 小程序前端 (apps/mini)

游戏代练平台微信小程序

## 📁 目录结构

```
src/
├── assets/                    # 静态资源
│   └── icons/                 # 图标文件
│
├── components/                # 组件
│   ├── common/               # 通用组件（按钮、输入框等）
│   └── business/             # 业务组件（订单卡片、游戏卡片等）
│
├── hooks/                     # 自定义 Hooks
│
├── pages/                     # 页面（按模块划分）
│   ├── home/                 # 首页模块
│   │   ├── index.tsx         # 页面组件
│   │   ├── index.scss        # 页面样式
│   │   └── index.config.ts   # 页面配置
│   │
│   ├── auth/                 # 认证模块
│   │   ├── login/            # 登录页
│   │   └── register/         # 注册页
│   │
│   ├── game/                 # 游戏模块
│   │   ├── list/             # 游戏列表
│   │   └── detail/           # 游戏详情
│   │
│   ├── order/                # 订单模块
│   │   ├── list/             # 订单列表
│   │   ├── create/           # 创建订单
│   │   └── detail/           # 订单详情
│   │
│   └── user/                 # 用户模块
│       ├── profile/          # 个人中心
│       ├── wallet/           # 钱包
│       └── booster-apply/    # 代练申请
│
├── services/                  # API 服务
│   ├── request.ts            # 请求封装
│   └── auth.service.ts       # 认证服务
│
├── store/                     # 状态管理
│
├── styles/                    # 全局样式
│   ├── variables.scss        # 变量定义
│   └── mixins.scss           # Mixins
│
├── types/                     # 类型定义
│
├── utils/                     # 工具函数
│   ├── index.ts              # 通用工具
│   └── storage.ts            # 存储工具
│
├── app.tsx                    # 应用入口
├── app.scss                   # 全局样式
└── app.config.ts              # 应用配置
```

## 🔧 开发命令

```bash
# 开发模式
pnpm dev:mini

# 编译构建
pnpm build:mini
```

## 📝 开发规范

### 页面命名
- 每个页面独立目录
- 文件名统一为 `index.tsx`、`index.scss`、`index.config.ts`
- 页面组件添加 `Page` 后缀，如 `LoginPage`

### 组件命名
- 通用组件放 `components/common/`
- 业务组件放 `components/business/`
- 组件文件名使用 PascalCase

### 样式规范
- 使用 SCSS
- 引入全局变量 `@import '@/styles/variables.scss'`
- 使用 BEM 命名或组件名作为前缀

