# 🚀 项目搭建完整指南

本文档详细说明如何从零开始搭建这个游戏代练平台项目。

## 📋 目录

- [项目概述](#项目概述)
- [环境要求](#环境要求)
- [第一步：安装基础工具](#第一步安装基础工具)
- [第二步：克隆项目](#第二步克隆项目)
- [第三步：安装依赖](#第三步安装依赖)
- [第四步：配置数据库](#第四步配置数据库)
- [第五步：配置环境变量](#第五步配置环境变量)
- [第六步：初始化数据库](#第六步初始化数据库)
- [第七步：启动项目](#第七步启动项目)
- [常见问题](#常见问题)

---

## 项目概述

这是一个基于 **Monorepo** 架构的全栈项目，使用 **pnpm workspace** 管理：

- **前端**：`apps/mini` - Taro 3.6 微信小程序
- **后端**：`apps/server` - Next.js 14 API 服务
- **数据库**：`packages/database` - Prisma ORM + PostgreSQL/SQLite
- **共享包**：`packages/shared` - 共享类型和工具函数

---

## 环境要求

在开始之前，请确保你的开发环境满足以下要求：

- **Node.js** >= 18.0.0
- **pnpm** >= 8.0.0
- **Git**
- **微信开发者工具**（用于小程序开发）
- **PostgreSQL 15+**（生产环境推荐）或 **SQLite**（开发环境）

### 检查环境版本

```bash
# 检查 Node.js 版本
node -v  # 应该 >= 18.0.0

# 检查 pnpm 版本
pnpm -v  # 应该 >= 8.0.0

# 如果没有安装 pnpm，使用以下命令安装
npm install -g pnpm
```

---

## 第一步：安装基础工具

### 1.1 安装 Node.js

如果还没有安装 Node.js，请访问 [Node.js 官网](https://nodejs.org/) 下载并安装 LTS 版本（推荐 18.x 或更高）。

### 1.2 安装 pnpm

```bash
# 使用 npm 全局安装 pnpm
npm install -g pnpm

# 或者使用 corepack（Node.js 16.13+ 自带）
corepack enable
corepack prepare pnpm@8.15.0 --activate
```

### 1.3 安装微信开发者工具

访问 [微信开发者工具官网](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html) 下载并安装。

---

## 第二步：克隆项目

```bash
# 克隆项目到本地
git clone <your-repo-url>
cd negro-mp
```

---

## 第三步：安装依赖

在项目根目录执行：

```bash
# 安装所有依赖（包括所有子项目的依赖）
pnpm install
```

这个命令会：
- 安装根目录的依赖
- 安装 `apps/mini` 的依赖
- 安装 `apps/server` 的依赖
- 安装 `packages/database` 的依赖
- 安装 `packages/shared` 的依赖

**注意**：如果安装过程中遇到网络问题，可以使用国内镜像：

```bash
# 使用淘宝镜像
pnpm config set registry https://registry.npmmirror.com
```

---

## 第四步：配置数据库

项目支持两种数据库：

### 方案 A：SQLite（开发环境推荐，简单快速）

SQLite 是文件数据库，无需安装额外的数据库服务，适合本地开发。

**优点**：
- 无需安装数据库服务
- 配置简单
- 适合快速开发

**缺点**：
- 不适合生产环境
- 并发性能有限

### 方案 B：PostgreSQL（生产环境推荐）

PostgreSQL 是功能强大的关系型数据库，适合生产环境。

**优点**：
- 性能优秀
- 功能完整
- 适合生产环境

**缺点**：
- 需要安装数据库服务
- 配置相对复杂

#### 安装 PostgreSQL（如果选择方案 B）

**macOS**：
```bash
# 使用 Homebrew
brew install postgresql@15
brew services start postgresql@15
```

**Linux (Ubuntu/Debian)**：
```bash
sudo apt update
sudo apt install postgresql-15 postgresql-contrib-15
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

**Windows**：
访问 [PostgreSQL 官网](https://www.postgresql.org/download/windows/) 下载安装包。

#### 创建数据库（PostgreSQL）

```bash
# 登录 PostgreSQL
psql -U postgres

# 创建数据库
CREATE DATABASE negro_mp;

# 创建用户（可选）
CREATE USER negro_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE negro_mp TO negro_user;

# 退出
\q
```

---

## 第五步：配置环境变量

### 5.1 创建环境变量文件

在项目根目录创建 `.env` 文件：

```bash
# 在项目根目录
touch .env
```

### 5.2 配置环境变量

编辑 `.env` 文件，填入以下配置：

#### 如果使用 SQLite（开发环境）

```env
# 数据库配置（SQLite）
DATABASE_URL="file:./dev.db"

# JWT 密钥（用于生成和验证 Token）
# ⚠️ 生产环境请使用强随机字符串
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# 微信小程序配置
# 从微信公众平台获取：https://mp.weixin.qq.com/
WECHAT_APP_ID="your-wechat-app-id"
WECHAT_APP_SECRET="your-wechat-app-secret"

# 可选：Prisma Accelerate（如果使用）
# DIRECT_URL="postgresql://user:password@localhost:5432/negro_mp?schema=public"
```

#### 如果使用 PostgreSQL（生产环境）

```env
# 数据库配置（PostgreSQL）
# 格式：postgresql://用户名:密码@主机:端口/数据库名?schema=public
DATABASE_URL="postgresql://negro_user:your_password@localhost:5432/negro_mp?schema=public"

# 如果使用 Prisma Accelerate，还需要配置 DIRECT_URL
# DIRECT_URL="postgresql://negro_user:your_password@localhost:5432/negro_mp?schema=public"

# JWT 密钥
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# 微信小程序配置
WECHAT_APP_ID="your-wechat-app-id"
WECHAT_APP_SECRET="your-wechat-app-secret"
```

### 5.3 获取微信小程序配置

1. 访问 [微信公众平台](https://mp.weixin.qq.com/)
2. 登录你的小程序账号
3. 进入「开发」->「开发管理」->「开发设置」
4. 找到「AppID(小程序ID)」和「AppSecret(小程序密钥)」
5. 将这两个值填入 `.env` 文件

**注意**：
- `.env` 文件包含敏感信息，**不要**提交到 Git
- 项目已配置 `.gitignore`，会自动忽略 `.env` 文件

---

## 第六步：初始化数据库

### 6.1 生成 Prisma 客户端

Prisma 需要根据 schema 文件生成客户端代码：

```bash
# 生成 Prisma 客户端
pnpm db:generate
```

这个命令会：
- 读取 `packages/database/prisma/schema.prisma` 文件
- 生成 TypeScript 类型定义
- 生成 Prisma Client 代码

### 6.2 推送数据库结构

将 Prisma schema 同步到数据库：

```bash
# 推送数据库结构（开发环境推荐）
pnpm db:push
```

**或者使用迁移（生产环境推荐）**：

```bash
# 创建迁移
pnpm db:migrate

# 这会创建一个新的迁移文件，并应用到数据库
```

**区别说明**：
- `db:push`：直接将 schema 同步到数据库，适合开发环境
- `db:migrate`：创建迁移文件，可以版本控制，适合生产环境

### 6.3 填充测试数据（可选）

如果需要测试数据，可以运行 seed 脚本：

```bash
# 填充测试数据
pnpm --filter @negro/database seed
```

这会创建：
- 游戏分类（王者荣耀、英雄联盟、和平精英等）
- 游戏区服
- 代练类型
- 测试用户
- 测试订单

### 6.4 打开 Prisma Studio（可选）

Prisma Studio 是一个可视化的数据库管理工具：

```bash
# 打开 Prisma Studio
pnpm db:studio
```

这会在浏览器中打开 `http://localhost:5555`，你可以在这里查看和编辑数据库数据。

---

## 第七步：启动项目

### 7.1 启动后端服务

在项目根目录执行：

```bash
# 启动后端 API 服务（端口 3000）
pnpm dev:server
```

这个命令会：
1. 自动生成 Prisma 客户端（如果还没有生成）
2. 启动 Next.js 开发服务器
3. 监听 `http://localhost:3000`

**验证后端是否启动成功**：
- 打开浏览器访问 `http://localhost:3000`
- 应该能看到 Next.js 的默认页面或 API 响应

### 7.2 启动小程序开发

**在另一个终端窗口**执行：

```bash
# 启动小程序开发构建
pnpm dev:mini
```

这个命令会：
1. 使用 Taro 编译小程序代码
2. 输出到 `apps/mini/dist` 目录
3. 监听文件变化，自动重新编译

### 7.3 在微信开发者工具中打开

1. 打开微信开发者工具
2. 点击「导入项目」
3. 选择项目目录：`apps/mini`
4. 填写 AppID（使用你在 `.env` 中配置的 `WECHAT_APP_ID`）
5. 点击「导入」

**注意**：
- 如果 AppID 填写错误，某些功能可能无法使用
- 开发环境可以使用「测试号」进行开发

### 7.4 配置小程序 API 地址

打开 `apps/mini/config/index.ts`，确保 API 地址指向你的后端服务：

```typescript
// 开发环境
const dev = {
  baseURL: 'http://localhost:3000',
  // 或者使用局域网 IP（手机调试时需要）
  // baseURL: 'http://192.168.1.100:3000',
};

// 生产环境
const prod = {
  baseURL: 'https://your-api-domain.com',
};
```

**手机调试注意事项**：
- 小程序无法访问 `localhost`
- 需要使用你的电脑的局域网 IP 地址
- 确保手机和电脑在同一网络
- 确保防火墙允许 3000 端口的连接

---

## 常见问题

### Q1: `pnpm install` 失败

**可能原因**：
- 网络问题
- Node.js 版本过低
- pnpm 版本不匹配

**解决方案**：
```bash
# 使用国内镜像
pnpm config set registry https://registry.npmmirror.com

# 清除缓存
pnpm store prune

# 重新安装
pnpm install
```

### Q2: Prisma 生成失败

**可能原因**：
- 数据库连接失败
- schema 文件有语法错误

**解决方案**：
```bash
# 检查 .env 文件中的 DATABASE_URL 是否正确
cat .env

# 检查 schema 文件
pnpm --filter @negro/database generate --schema=./packages/database/prisma/schema.prisma
```

### Q3: 数据库连接失败（PostgreSQL）

**可能原因**：
- PostgreSQL 服务未启动
- 用户名/密码错误
- 数据库不存在

**解决方案**：
```bash
# 检查 PostgreSQL 服务状态
# macOS
brew services list

# Linux
sudo systemctl status postgresql

# 测试连接
psql -U postgres -d negro_mp

# 如果连接失败，检查 .env 中的 DATABASE_URL
```

### Q4: 小程序无法访问后端 API

**可能原因**：
- 使用了 `localhost`（小程序无法访问）
- 后端服务未启动
- 网络配置问题

**解决方案**：
1. 获取电脑的局域网 IP：
   ```bash
   # macOS/Linux
   ifconfig | grep "inet "
   
   # Windows
   ipconfig
   ```

2. 修改 `apps/mini/config/index.ts`，使用局域网 IP：
   ```typescript
   baseURL: 'http://192.168.1.100:3000', // 替换为你的 IP
   ```

3. 确保后端服务正在运行：
   ```bash
   pnpm dev:server
   ```

4. 在微信开发者工具中，点击「详情」->「本地设置」-> 勾选「不校验合法域名」

### Q5: 端口被占用

**可能原因**：
- 3000 端口已被其他程序占用

**解决方案**：
```bash
# 查找占用端口的进程
# macOS/Linux
lsof -i :3000

# Windows
netstat -ano | findstr :3000

# 杀死进程（替换 PID 为实际进程 ID）
kill -9 <PID>
```

或者修改端口：
```bash
# 修改 apps/server/package.json 中的 dev 脚本
"dev": "next dev -p 3001"
```

### Q6: Prisma Studio 无法打开

**可能原因**：
- 数据库连接失败
- 端口被占用

**解决方案**：
```bash
# 检查数据库连接
pnpm db:push

# 使用自定义端口
pnpm --filter @negro/database studio --port 5556
```

---

## 📚 下一步

项目搭建完成后，你可以：

1. **查看 API 文档**：访问 `http://localhost:3000/api` 查看可用接口
2. **查看数据库**：运行 `pnpm db:studio` 可视化查看数据
3. **阅读代码**：查看 `apps/server/src/app/api` 了解 API 实现
4. **开发功能**：根据业务需求添加新功能

---

## 🆘 获取帮助

如果遇到问题：

1. 检查本文档的「常见问题」部分
2. 查看项目的 README.md
3. 检查 GitHub Issues
4. 联系项目维护者

---

**祝你开发愉快！🎉**

